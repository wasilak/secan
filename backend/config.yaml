server:
  host: "127.0.0.1"
  port: 9000

auth:
  mode: "open"
  session_timeout_minutes: 30

clusters:
  - id: "local"
    name: "Local Development"
    nodes:
      - "http://localhost:9200"
    client_type: "sdk"
    version_hint: "8"
