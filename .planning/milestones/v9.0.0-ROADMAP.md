# Milestone Archive: v9.0.0 — Templates & Aliases

**Status:** ✅ Shipped
**Shipped:** 2026-05-24
**Phases:** 2 | **Plans:** 4 | **Branch:** templates-and-aliases

---

## What Was Delivered

Full CRUD support for index templates, component templates, and index aliases in the secan UI, plus a detail modal with Monaco editors, POST _simulate diff view, and dnd-kit drag-and-drop component selector. All management surfaces are now tabs within the Indices view rather than separate nav items, matching the PRD's intended IA.

---

## Phases

### Phase 1: Component Templates Foundation

**Goal:** Alias backend committed, ComponentTemplates page shipped, both builds clean.
**Mode:** mvp
**Status:** Complete (plans executed before GSD planning was set up; commits preserved in git history)

**Success Criteria:**
1. ✅ Component templates list at `/cluster/:id/indices?indicesTab=component-templates`
2. ✅ Operator can create a new component template (name, version, JSON settings/mappings)
3. ✅ Operator can delete a component template with confirmation
4. ✅ Templates, Component Templates, Aliases accessible as tabs in Indices view
5. ✅ `cargo build` and `npm run build` both pass with no errors

**Key commits:**
- `78e67439` feat: commit alias route handlers
- `47a3be1b` feat(frontend): add ComponentTemplates page, sidebar nav, router entry

---

### Phase 2: Template Detail, Simulate & Routing

**Goal:** Operators can inspect full template details with Monaco editors, simulate resulting index settings and see a diff, use drag-and-drop to compose templates, and share modal state via URL.
**Mode:** mvp
**Status:** Complete — 4/4 plans

**Plans:**
- 02-01: Install npm deps + extend types + API client (Wave 1) ✅
- 02-02: TemplateDetailsModal four tabs + Templates.tsx row-click + URL routing (Wave 2) ✅
- 02-03: Simulate diff view + dnd-kit sortable Composed tab (Wave 3) ✅
- 02-04: Frontend build + cargo build verification (Wave 4) ✅

**Success Criteria:**
1. ✅ Clicking a template row opens TemplateDetailsModal with Overview, Settings, Mappings, Composed tabs
2. ✅ Settings and Mappings tabs show read-only JSON in Monaco editor
3. ✅ Simulate button calls `POST _simulate` and displays unified diff
4. ✅ Composable templates have dnd-kit drag-and-drop component selector
5. ✅ Opening a template detail modal updates URL with `?templateModal=` query param

**Key commits:**
- `f746bd4d` chore(deps): install @dnd-kit/*, react-diff-view, diff
- `914c162a` feat(types): add TemplateDetail, SimulateTemplateRequest, SimulateTemplateResponse
- `f7901f07` feat(api): add getTemplate, simulateTemplate methods
- `7efefde0` feat(frontend): add TemplateDetailsModal with four tabs
- `7ac9599c` feat(frontend): wire Templates.tsx for row-click and URL routing
- `88d5ead4` feat(templates): add simulate diff and dnd sortable composed tab
- `af4516e6` fix(build): remove unused imports from templates router and server
- `67035e63` refactor(nav): move templates/component-templates/aliases into Indices tab view
- `3db29db3` fix(templates): hide NumberInput spinners in Create Template modal

---

## Milestone Stats

- **Timeline:** 2026-05-24 (single day)
- **Commits:** 11 TMPL-001 commits
- **Files changed:** 12 source files (TS/TSX/RS), +1019/-49 lines
- **npm deps added:** @dnd-kit/core, @dnd-kit/sortable, @dnd-kit/utilities, react-diff-view, diff (5 packages)

## Key Decisions

| Decision | Rationale | Outcome |
|----------|-----------|---------|
| Tabs in Indices view (not separate nav items) | PRD intent — index management under Indices, not scattered nav | ✓ Implemented via `?indicesTab=` query param |
| dnd-kit for component selector | Best-in-class React DnD, already referenced in PRD | ✓ Sortable display-only reorder |
| react-diff-view for simulate diff | Lightweight unified diff renderer with CSS theming | ✓ Before/after JSON diff |
| Query-param routing for modals | Matches existing codebase patterns; URL-shareable | ✓ `?templateModal=<name>` |
| TemplateDetailsModal read-only first | Simpler MVP; inline editing deferred to v2 | ✓ Read-only Monaco tabs |
| `embedded` prop on page components | Skip FullWidthContainer (viewport-aware width) when rendering as tabs inside ClusterView | ✓ No layout double-wrapping |

## Tech Debt / Deferred

- BACK-02: Task Master task 2 status not updated programmatically (minor)
- dnd-kit Composed tab is display-only reorder — no PUT to save order (deferred to v2 per SIM-04 decision)
- Inline JSON editing in TemplateDetailsModal not yet available (ADV-01 in v2 requirements)
- No integration tests for alias/template route handlers (TEST-01, TEST-02 in v2 requirements)

---
*Archived: 2026-05-24*
